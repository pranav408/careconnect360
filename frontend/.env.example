# Same-origin deployment or local Vite proxy
VITE_API_BASE_URL=/api

# For split-origin deployments, set the complete API root ending with /api
# Example:
# VITE_API_BASE_URL=https://api.example.com/api

# Never store secrets in VITE_* variables.
# VITE_* values are embedded into the browser bundle at build time.
