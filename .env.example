# Copy this file to .env before startup. The .env file is ignored by git.
# Replace all <CHANGE_ME...> placeholders with local secrets.
# VITE_* values are public in browser bundles; never store secrets in VITE_*.
# Full Docker Compose frontend always builds with /api for same-origin routing.

SPRING_PROFILES_ACTIVE=local
DB_URL=jdbc:mysql://localhost:3307/careconnect360?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_USERNAME=<LOCAL_DB_USERNAME>
DB_PASSWORD=<LOCAL_DB_PASSWORD>
JWT_SECRET=<CHANGE_ME_TO_A_LONG_RANDOM_SECRET>
JWT_EXPIRATION_MS=3600000
ADMIN_EMAIL=admin@careconnect360.com
ADMIN_PASSWORD=<LOCAL_ADMIN_PASSWORD>
SERVER_PORT=8080
FLYWAY_ENABLED=true
FLYWAY_BASELINE_ON_MIGRATE=false
APP_CORS_ALLOWED_ORIGINS=http://localhost:8088
APP_DOCS_ENABLED=true
APP_ADMIN_BOOTSTRAP_ENABLED=false
MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info
MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS=always
MYSQL_DATABASE=careconnect360
MYSQL_USER=careconnect
MYSQL_PASSWORD=<CHANGE_ME>
MYSQL_ROOT_PASSWORD=<CHANGE_ME>
MYSQL_HOST_PORT=3307
FRONTEND_HOST_PORT=8088
